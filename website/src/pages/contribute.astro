---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Claudia — Teach her something">
  <section class="pt-32 pb-20 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal mb-6">
        <img src="/assets/claudia-mascot.png" alt="" class="w-16 h-16 object-contain" />
      </div>
      <p class="reveal font-mono text-xs text-vermillion tracking-widest uppercase mb-4">Open source</p>
      <h1 class="reveal text-4xl sm:text-5xl font-extrabold tracking-[-0.03em] leading-[1.05]">
        Know something?<br />Teach it to Claudia.
      </h1>
      <p class="reveal mt-5 text-paper/40 text-lg font-light max-w-lg">
        Contributing means writing a markdown file. That's it. No code review, no tests, no build step. If you can explain something about technology, you can make Claudia smarter.
      </p>
    </div>
  </section>

  <!-- Contribution levels -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <p class="reveal font-mono text-xs text-paper/30 tracking-widest uppercase mb-8">Pick your level</p>

      <div class="space-y-px stagger">
        {[
          { title: "Write what you know", time: "~30 min", desc: "Drop a markdown file in any references/ folder. Know a lot about Redis? Write about Redis. That's the whole contribution." },
          { title: "Teach her to catch mistakes", time: "~1 hour", desc: "Add patterns to the hook scripts. If you've seen a common mistake, teach Claudia to catch it for everyone." },
          { title: "Make a topic deeper", time: "~2 hours", desc: "Pick a domain Claudia already knows and make her advice better. Add examples, edge cases, real-world gotchas." },
          { title: "Add a whole new topic", time: "~4 hours", desc: "Use the scaffolding tool to create a full knowledge domain. Own a subject — architecture, mobile, ML, whatever you know." },
          { title: "Build a new automatic check", time: "~4 hours", desc: "Write a script that catches an issue every time Claude writes a file. Security, quality, accessibility — anything that should never slip through." },
        ].map((item) => (
          <div class="reveal bg-white/[0.02] border border-white/[0.04] rounded-lg p-6 hover:border-vermillion/20 transition-colors group">
            <div class="flex items-start justify-between gap-4">
              <div>
                <h3 class="text-base font-semibold text-paper/90 group-hover:text-vermillion transition-colors">{item.title}</h3>
                <p class="mt-2 text-sm text-paper/35 leading-relaxed">{item.desc}</p>
              </div>
              <span class="shrink-0 text-[11px] font-mono text-vermillion/60 bg-vermillion/[0.08] px-2.5 py-1 rounded-full">{item.time}</span>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Quick start -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <p class="reveal font-mono text-xs text-paper/30 tracking-widest uppercase mb-8">Quick start</p>

      <div class="reveal rounded-xl overflow-hidden border border-white/[0.08]">
        <div class="bg-[#1a1a1e] px-4 py-3 flex items-center gap-2">
          <span class="w-2.5 h-2.5 rounded-full bg-[#ff5f57]"></span>
          <span class="w-2.5 h-2.5 rounded-full bg-[#febc2e]"></span>
          <span class="w-2.5 h-2.5 rounded-full bg-[#28c840]"></span>
        </div>
        <pre class="bg-[#141417] px-6 py-5 text-[13px] font-mono leading-[1.9] overflow-x-auto"><code><span class="text-[#28c840]">git clone</span> <span class="text-paper/60">https://github.com/reganomalley/claudia.git</span>
<span class="text-[#28c840]">cd</span> <span class="text-paper/60">claudia</span>
<span class="text-[#28c840]">./scripts/create-domain.sh</span> <span class="text-paper/60">mobile</span>
<span class="text-paper/25"># Now just write markdown in skills/claudia-mobile/</span>
<span class="text-paper/25"># That's literally it. Open a PR when you're done.</span></code></pre>
      </div>
    </div>
  </section>

  <!-- Writing guidelines -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <p class="reveal font-mono text-xs text-paper/30 tracking-widest uppercase mb-8">How to write for Claudia</p>

      <div class="grid sm:grid-cols-2 gap-px bg-white/[0.06] rounded-xl overflow-hidden stagger">
        {[
          { title: "Have an opinion", desc: "Don't say 'it depends.' Say what you'd actually use, then explain the tradeoffs so people can disagree." },
          { title: "Lead with the answer", desc: "Put the recommendation first. Then the reasoning. People are here because they need to make a decision, not learn the theory." },
          { title: "Say when NOT to use it", desc: "Every tool breaks somewhere. Saying where builds trust and saves people hours of debugging the wrong choice." },
          { title: "Use tables and decision trees", desc: "A comparison table is worth a thousand words. If someone is choosing between two things, make a table." },
        ].map((g) => (
          <div class="reveal bg-void p-7">
            <h3 class="text-sm font-semibold text-vermillion">{g.title}</h3>
            <p class="mt-2 text-xs text-paper/35 leading-relaxed">{g.desc}</p>
          </div>
        ))}
        <div class="reveal bg-void p-7 sm:col-span-2">
          <h3 class="text-sm font-semibold text-vermillion">Keep it tight</h3>
          <p class="mt-2 text-xs text-paper/35 leading-relaxed">
            The main file loads into Claude's memory every time. Keep it under 2000 words. Put deep dives, reference tables, and long examples into the <code class="text-vermillion/60">references/</code> folder — those only load when needed.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Security model -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <p class="reveal font-mono text-xs text-[#ff5f57] tracking-widest uppercase mb-8">Before you contribute</p>

      <div class="reveal border border-[#ff5f57]/20 bg-[#ff5f57]/[0.03] rounded-xl p-8">
        <h3 class="text-lg font-semibold text-paper/90 mb-4">Hooks run on real machines</h3>
        <p class="text-sm text-paper/40 leading-relaxed mb-6">
          Claudia's hook scripts execute locally with the same permissions as Claude Code. A malicious hook could read files, exfiltrate data, or modify code silently. We take this seriously.
        </p>

        <div class="space-y-4">
          <div class="flex items-start gap-3">
            <span class="shrink-0 mt-0.5 text-[10px] font-mono font-medium px-2 py-0.5 rounded-full bg-[#ff5f57]/20 text-[#ff5f57]">Rule</span>
            <p class="text-sm text-paper/50"><strong class="text-paper/70">All hook and script PRs require maintainer review.</strong> Markdown-only contributions to references/ are lower risk, but hooks touch every user's machine.</p>
          </div>
          <div class="flex items-start gap-3">
            <span class="shrink-0 mt-0.5 text-[10px] font-mono font-medium px-2 py-0.5 rounded-full bg-[#ff5f57]/20 text-[#ff5f57]">Rule</span>
            <p class="text-sm text-paper/50"><strong class="text-paper/70">No network calls in hooks.</strong> Hook scripts must never fetch URLs, phone home, or access anything outside the local filesystem.</p>
          </div>
          <div class="flex items-start gap-3">
            <span class="shrink-0 mt-0.5 text-[10px] font-mono font-medium px-2 py-0.5 rounded-full bg-[#ff5f57]/20 text-[#ff5f57]">Rule</span>
            <p class="text-sm text-paper/50"><strong class="text-paper/70">No obfuscated code.</strong> Every line should be readable. If a reviewer can't understand what a script does, it doesn't ship.</p>
          </div>
          <div class="flex items-start gap-3">
            <span class="shrink-0 mt-0.5 text-[10px] font-mono font-medium px-2 py-0.5 rounded-full bg-vermillion/20 text-vermillion">How</span>
            <p class="text-sm text-paper/50">Hook PRs get a dedicated security review. We diff every line against the existing script, check for exfil patterns, and test in a sandboxed environment before merging.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="pb-20 px-6">
    <div class="max-w-4xl mx-auto text-center">
      <a
        href="https://github.com/reganomalley/claudia"
        target="_blank"
        rel="noopener noreferrer"
        class="reveal inline-flex items-center gap-2 bg-vermillion hover:bg-vermillion/90 text-paper text-sm font-medium px-6 py-2.5 rounded-lg transition-colors"
      >
        Start contributing
        <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" />
        </svg>
      </a>
    </div>
  </section>
</Layout>
