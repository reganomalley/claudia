---
import Layout from "../layouts/Layout.astro";
---

<Layout title="Claudia — Start here" description="Go from zero to building with Claude Code in 10 minutes. No coding experience required.">

  <!-- ===================== HERO ===================== -->
  <section class="pt-32 pb-20 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal mb-6">
        <img src="/assets/claudia-mascot.png" alt="" class="w-16 h-16 object-contain" />
      </div>
      <p class="reveal font-mono text-xs text-vermillion tracking-widest uppercase mb-4">Start here</p>
      <h1 class="reveal text-5xl sm:text-6xl lg:text-7xl font-extrabold tracking-[-0.04em] leading-[0.9]">
        Your first<br />10 minutes.
      </h1>
      <p class="reveal mt-6 text-paper/40 text-lg font-light max-w-lg leading-relaxed">
        You don't need to be a developer. You need a terminal, an API key, and 10 minutes. Claudia handles the rest.
      </p>
    </div>
  </section>

  <!-- ===================== STEP 0: TERMINAL ===================== -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal flex items-baseline gap-4 mb-8">
        <span class="text-5xl font-extrabold text-vermillion/20 leading-none">00</span>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Open your terminal</h2>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        A terminal is just a text box where you type commands. That's it. No GUI, no buttons — you type something, press Enter, and it does it.
      </p>

      <!-- Platform tabs -->
      <div class="reveal" data-tabs>
        <div class="flex gap-1 mb-4">
          <button class="tab-btn active" data-tab="mac">Mac</button>
          <button class="tab-btn" data-tab="windows">Windows</button>
          <button class="tab-btn" data-tab="linux">Linux</button>
        </div>

        <div class="tab-panel active" data-panel="mac">
          <div class="border border-white/[0.06] bg-white/[0.015] rounded-xl p-6">
            <p class="text-sm text-paper/50 mb-3">Press <kbd class="kbd">Cmd</kbd> + <kbd class="kbd">Space</kbd>, type <strong class="text-paper/70">Terminal</strong>, hit Enter.</p>
            <p class="text-xs text-paper/30">You'll see a window with a blinking cursor. That's your terminal. If you prefer a nicer one later, look up iTerm2 — but the built-in one works fine.</p>
          </div>
        </div>
        <div class="tab-panel" data-panel="windows">
          <div class="border border-white/[0.06] bg-white/[0.015] rounded-xl p-6">
            <p class="text-sm text-paper/50 mb-3">Press <kbd class="kbd">Win</kbd>, type <strong class="text-paper/70">PowerShell</strong>, hit Enter.</p>
            <p class="text-xs text-paper/30">Use PowerShell, not Command Prompt (CMD). PowerShell is the modern one. If you see a blue window, you're in the right place.</p>
          </div>
        </div>
        <div class="tab-panel" data-panel="linux">
          <div class="border border-white/[0.06] bg-white/[0.015] rounded-xl p-6">
            <p class="text-sm text-paper/50 mb-3">Press <kbd class="kbd">Ctrl</kbd> + <kbd class="kbd">Alt</kbd> + <kbd class="kbd">T</kbd>.</p>
            <p class="text-xs text-paper/30">On most Linux distributions, this opens the default terminal. If it doesn't, look for "Terminal" in your applications menu.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== STEP 1: NODE ===================== -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal flex items-baseline gap-4 mb-8">
        <span class="text-5xl font-extrabold text-vermillion/20 leading-none">01</span>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Install Node.js</h2>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        Node.js lets you run JavaScript tools on your computer. Claude Code is one of those tools. You install it once and forget about it.
      </p>

      <div class="reveal" data-tabs>
        <div class="flex gap-1 mb-4">
          <button class="tab-btn active" data-tab="mac-node">Mac</button>
          <button class="tab-btn" data-tab="windows-node">Windows</button>
          <button class="tab-btn" data-tab="linux-node">Linux</button>
        </div>

        <div class="tab-panel active" data-panel="mac-node">
          <div class="border border-white/[0.06] bg-white/[0.015] rounded-xl p-6 space-y-4">
            <p class="text-sm text-paper/50">Option A — if you have Homebrew:</p>
            <div class="bg-black/40 rounded-lg p-4 font-mono text-sm">
              <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">brew install node</span>
            </div>
            <p class="text-sm text-paper/50">Option B — download directly:</p>
            <p class="text-sm text-paper/40">Go to <a href="https://nodejs.org" target="_blank" rel="noopener noreferrer" class="text-vermillion/70 hover:text-vermillion transition-colors underline underline-offset-2">nodejs.org</a> and download the LTS version. Run the installer.</p>
          </div>
        </div>
        <div class="tab-panel" data-panel="windows-node">
          <div class="border border-white/[0.06] bg-white/[0.015] rounded-xl p-6 space-y-4">
            <p class="text-sm text-paper/50">Download from <a href="https://nodejs.org" target="_blank" rel="noopener noreferrer" class="text-vermillion/70 hover:text-vermillion transition-colors underline underline-offset-2">nodejs.org</a> — click the big green LTS button.</p>
            <p class="text-sm text-paper/40">Run the installer, click Next through everything. The defaults are fine.</p>
          </div>
        </div>
        <div class="tab-panel" data-panel="linux-node">
          <div class="border border-white/[0.06] bg-white/[0.015] rounded-xl p-6 space-y-4">
            <div class="bg-black/40 rounded-lg p-4 font-mono text-sm leading-relaxed">
              <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -</span><br/>
              <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">sudo apt-get install -y nodejs</span>
            </div>
            <p class="text-xs text-paper/30">For Fedora/RHEL, use <code class="text-paper/50">dnf install nodejs</code> instead.</p>
          </div>
        </div>
      </div>

      <!-- Verify -->
      <div class="reveal mt-6">
        <p class="text-sm text-paper/50 mb-3">Verify it worked:</p>
        <div class="rounded-2xl overflow-hidden border border-white/[0.08]">
          <div class="bg-[#1a1a1e] px-4 py-2.5 flex items-center gap-2">
            <span class="w-2.5 h-2.5 rounded-full bg-[#ff5f57]"></span>
            <span class="w-2.5 h-2.5 rounded-full bg-[#febc2e]"></span>
            <span class="w-2.5 h-2.5 rounded-full bg-[#28c840]"></span>
          </div>
          <div class="bg-[#111114] px-6 py-4 font-mono text-sm leading-[2]">
            <div><span class="text-[#28c840]">$</span> <span class="text-paper/60">node --version</span></div>
            <div><span class="text-paper/40">v22.12.0</span></div>
          </div>
        </div>
        <p class="text-xs text-paper/25 mt-3">The version number doesn't have to match exactly — anything v18 or higher is fine.</p>
      </div>

      <!-- Troubleshooting -->
      <div class="reveal mt-6">
        <details class="troubleshoot">
          <summary>"command not found" — Node didn't install?</summary>
          <div class="px-5 pb-4 text-sm text-paper/40 space-y-2">
            <p><strong class="text-paper/60">Close your terminal and open a new one.</strong> New tools often aren't available until you restart the terminal.</p>
            <p>If it still doesn't work, the installer might not have added Node to your PATH. On Mac, try running <code class="text-paper/50">export PATH="/usr/local/bin:$PATH"</code> and then <code class="text-paper/50">node --version</code> again.</p>
            <p>On Windows, restart your computer — the PATH changes sometimes need a full reboot.</p>
          </div>
        </details>
      </div>
    </div>
  </section>

  <!-- ===================== STEP 2: API KEY ===================== -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal flex items-baseline gap-4 mb-8">
        <span class="text-5xl font-extrabold text-vermillion/20 leading-none">02</span>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Get a Claude API key</h2>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        An API key is like a password that lets Claude Code talk to Claude. You create one on Anthropic's website.
      </p>

      <div class="reveal border border-white/[0.06] bg-white/[0.015] rounded-xl p-6 space-y-5">
        <div class="space-y-4">
          <div class="flex gap-3">
            <span class="shrink-0 w-6 h-6 rounded-full bg-vermillion/10 text-vermillion text-xs font-bold flex items-center justify-center">1</span>
            <p class="text-sm text-paper/50">Go to <a href="https://console.anthropic.com" target="_blank" rel="noopener noreferrer" class="text-vermillion/70 hover:text-vermillion transition-colors underline underline-offset-2">console.anthropic.com</a> and create an account (or sign in).</p>
          </div>
          <div class="flex gap-3">
            <span class="shrink-0 w-6 h-6 rounded-full bg-vermillion/10 text-vermillion text-xs font-bold flex items-center justify-center">2</span>
            <p class="text-sm text-paper/50">Click <strong class="text-paper/70">API Keys</strong> in the left sidebar.</p>
          </div>
          <div class="flex gap-3">
            <span class="shrink-0 w-6 h-6 rounded-full bg-vermillion/10 text-vermillion text-xs font-bold flex items-center justify-center">3</span>
            <p class="text-sm text-paper/50">Click <strong class="text-paper/70">Create Key</strong>. Give it a name like "claude-code".</p>
          </div>
          <div class="flex gap-3">
            <span class="shrink-0 w-6 h-6 rounded-full bg-vermillion/10 text-vermillion text-xs font-bold flex items-center justify-center">4</span>
            <p class="text-sm text-paper/50">Copy the key. It starts with <code class="text-paper/60">sk-ant-</code>. <strong class="text-paper/70">Keep this tab open</strong> — you'll need it in the next step.</p>
          </div>
        </div>
      </div>

      <div class="reveal mt-4 border border-signal/20 bg-signal/[0.03] rounded-xl px-5 py-3">
        <p class="text-xs text-paper/40"><strong class="text-signal/70">Security note:</strong> This key is like a password. Don't share it, don't paste it in your code, and don't commit it to GitHub. Claude Code stores it securely for you.</p>
      </div>
    </div>
  </section>

  <!-- ===================== STEP 3: CLAUDE CODE ===================== -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal flex items-baseline gap-4 mb-8">
        <span class="text-5xl font-extrabold text-vermillion/20 leading-none">03</span>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Install Claude Code</h2>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        Claude Code is the tool that lets you talk to Claude in your terminal. One command installs it.
      </p>

      <div class="reveal bg-black/40 rounded-lg p-4 font-mono text-sm mb-4">
        <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">npm install -g @anthropic-ai/claude-code</span>
      </div>

      <p class="reveal text-sm text-paper/40 mb-4">Then start it:</p>

      <div class="reveal bg-black/40 rounded-lg p-4 font-mono text-sm mb-4">
        <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">claude</span>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        The first time you run it, Claude Code will ask for your API key. Paste the one you copied in the previous step.
      </p>

      <!-- What you should see -->
      <div class="reveal">
        <p class="text-sm text-paper/50 mb-3">What you should see:</p>
        <div class="rounded-2xl overflow-hidden border border-white/[0.08]">
          <div class="bg-[#1a1a1e] px-4 py-2.5 flex items-center gap-2">
            <span class="w-2.5 h-2.5 rounded-full bg-[#ff5f57]"></span>
            <span class="w-2.5 h-2.5 rounded-full bg-[#febc2e]"></span>
            <span class="w-2.5 h-2.5 rounded-full bg-[#28c840]"></span>
          </div>
          <div class="bg-[#111114] px-6 py-4 font-mono text-sm leading-[2]">
            <div><span class="text-[#28c840]">$</span> <span class="text-paper/60">claude</span></div>
            <div><span class="text-paper/40">Welcome to Claude Code!</span></div>
            <div><span class="text-paper/40">Type your message to get started.</span></div>
            <div>&nbsp;</div>
            <div><span class="text-paper/30">></span> <span class="text-paper/20 cursor">_</span></div>
          </div>
        </div>
      </div>

      <div class="reveal mt-6">
        <details class="troubleshoot">
          <summary>Permission errors on Mac/Linux?</summary>
          <div class="px-5 pb-4 text-sm text-paper/40 space-y-2">
            <p>If you see <code class="text-paper/50">EACCES</code> permission errors, your npm global directory needs fixing. Run:</p>
            <div class="bg-black/40 rounded-lg p-3 font-mono text-xs">
              <span class="text-paper/30 select-none">$ </span><span class="text-paper/70">mkdir -p ~/.npm-global && npm config set prefix '~/.npm-global'</span>
            </div>
            <p>Then add this to your shell profile (<code class="text-paper/50">~/.zshrc</code> or <code class="text-paper/50">~/.bashrc</code>):</p>
            <div class="bg-black/40 rounded-lg p-3 font-mono text-xs">
              <span class="text-paper/70">export PATH=~/.npm-global/bin:$PATH</span>
            </div>
            <p>Restart your terminal and try the install again.</p>
          </div>
        </details>
      </div>
    </div>
  </section>

  <!-- ===================== STEP 4: CLAUDIA ===================== -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal flex items-baseline gap-4 mb-8">
        <span class="text-5xl font-extrabold text-vermillion/20 leading-none">04</span>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Install Claudia</h2>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        Claudia is a plugin that makes Claude Code smarter. She catches security issues, teaches you as you go, and makes sure the code Claude writes is actually good.
      </p>

      <div class="reveal bg-black/40 rounded-lg p-4 font-mono text-sm mb-6">
        <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">npm install -g claudia-mentor</span>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-4">
        Restart your terminal, then start Claude Code again:
      </p>

      <div class="reveal bg-black/40 rounded-lg p-4 font-mono text-sm mb-6">
        <span class="text-paper/30 select-none">$ </span><span class="text-paper/80">claude</span>
      </div>

      <div class="reveal">
        <p class="text-sm text-paper/50 mb-3">You should see Claudia's greeting:</p>
        <div class="rounded-2xl overflow-hidden border border-white/[0.08]">
          <div class="bg-[#1a1a1e] px-4 py-2.5 flex items-center gap-2">
            <span class="w-2.5 h-2.5 rounded-full bg-[#ff5f57]"></span>
            <span class="w-2.5 h-2.5 rounded-full bg-[#febc2e]"></span>
            <span class="w-2.5 h-2.5 rounded-full bg-[#28c840]"></span>
          </div>
          <div class="bg-[#111114] px-6 py-5 font-mono text-[13px] leading-none">
            <!-- Pixel art mascot (matches postinstall.js output) -->
            <!-- Colors: #303030=hair, #484848=hair-hi, #ff0000=red, #d70000=dark-red, #fff=eyes, #000=pupils, #ff5f5f=hands/coral, #5f0000=mouth -->
            <div class="mascot-art mb-1" style="letter-spacing: 0;">
              <div>      <span style="color:#ff5f5f">*</span></div>
              <div>   <span style="color:#303030">▀</span><span style="color:#484848;background:#303030">▀</span><span style="color:#303030;background:#303030">█</span><span style="color:#303030">▀</span>      </div>
              <div>  <span style="color:#303030;background:#303030">█</span><span style="color:#303030;background:#303030">█</span><span style="color:#303030;background:#ff0000">▀</span><span style="color:#303030;background:#ff0000">▀</span><span style="color:#303030;background:#ff0000">▀</span><span style="color:#303030;background:#ff0000">▀</span><span style="color:#303030;background:#303030">█</span><span style="color:#303030;background:#303030">█</span>    </div>
              <div>  <span style="color:#303030;background:#303030">█</span><span style="color:#ff0000;background:#ff0000">█</span><span style="color:#fff;background:#fff">█</span><span style="color:#000;background:#000">█</span><span style="color:#ff0000;background:#ff0000">█</span><span style="color:#fff;background:#fff">█</span><span style="color:#000;background:#000">█</span><span style="color:#303030;background:#303030">█</span>    </div>
              <div>   <span style="color:#ff0000;background:#ff0000">█</span><span style="color:#ff0000;background:#ff0000">█</span><span style="color:#5f0000;background:#5f0000">█</span><span style="color:#5f0000;background:#d70000">▀</span><span style="color:#ff0000;background:#ff0000">█</span><span style="color:#ff0000;background:#d70000">▀</span>     </div>
              <div>  <span style="color:#ff5f5f">▀</span>      <span style="color:#ff5f5f">▀</span>    </div>
            </div>

            <div class="leading-[1.8] mt-3">
              <div>  <span class="text-paper/70 font-bold">Claudia</span> <span class="text-paper/25">v0.4.0</span></div>
              <div>  <span class="text-paper/30">She catches what you miss.</span></div>
              <div>&nbsp;</div>
              <div>  <span class="text-[#28c840]">+</span> <span class="text-paper/60">Auto-configured.</span> <span class="text-paper/30">Restart your terminal, then just type</span> <span class="text-paper/70">claude</span></div>
              <div>&nbsp;</div>
              <div>  <span class="text-paper/30">She checks your code automatically.</span></div>
              <div>  <span class="text-paper/30">She joins conversations about tech decisions.</span></div>
              <div>  <span class="text-paper/30">She teaches you as you build.</span></div>
              <div>&nbsp;</div>
              <div>  <span class="text-paper/25">Docs:</span>   <span class="text-paper/60">https://askclaudia.dev</span></div>
              <div>  <span class="text-paper/25">Source:</span> <span class="text-paper/60">https://github.com/reganomalley/claudia</span></div>
              <div>&nbsp;</div>
              <div>  <span class="text-paper/30">Every Claude needs a Claudia.</span></div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ===================== STEP 5: MAKE IT YOURS ===================== -->
  <section class="pb-28 px-6">
    <div class="max-w-4xl mx-auto">
      <div class="reveal flex items-baseline gap-4 mb-8">
        <span class="text-5xl font-extrabold text-vermillion/20 leading-none">05</span>
        <h2 class="text-2xl sm:text-3xl font-bold tracking-tight">Make it yours <span class="text-paper/20 text-xl font-normal">(optional)</span></h2>
      </div>

      <p class="reveal text-sm text-paper/40 leading-relaxed mb-6">
        Your terminal is going to be your workspace. It's worth making it comfortable — a better font, a better terminal app, maybe a color theme. But you don't have to figure that out yourself.
      </p>

      <div class="reveal border border-white/[0.06] bg-white/[0.015] rounded-xl p-6">
        <p class="text-sm text-paper/50 mb-4">Just ask Claudia. She'll detect what you're running and walk you through it:</p>
        <div class="bg-black/40 rounded-lg p-4 font-mono text-sm mb-4">
          <span class="text-paper/30 select-none">> </span><span class="text-paper/60">/claudia:ask</span> <span class="text-paper/40">how do I make my terminal look nicer?</span>
        </div>
        <p class="text-xs text-paper/30 leading-relaxed">
          She'll recommend a font, a terminal app for your OS, and help you install and configure them — step by step, in your terminal, right now.
        </p>
      </div>
    </div>
  </section>

  <!-- ===================== CTA ===================== -->
  <section class="relative py-32 sm:py-40 overflow-hidden">
    <div class="absolute inset-0 bg-vermillion"></div>

    <div class="relative max-w-4xl mx-auto px-6 text-center">
      <h2 class="reveal text-4xl sm:text-5xl lg:text-6xl font-extrabold tracking-[-0.04em] leading-[0.9] text-void">
        You're ready.
      </h2>
      <p class="reveal mt-6 text-void/60 text-lg font-light max-w-md mx-auto leading-relaxed">
        Open Claude Code and run your first guided session. Claudia will walk you through everything.
      </p>

      <div class="reveal mt-10">
        <div class="inline-block bg-void/90 backdrop-blur rounded-xl px-6 py-4 font-mono text-sm text-paper/80">
          <span class="text-vermillion mr-2">></span>/claudia:setup
        </div>
      </div>

      <p class="reveal mt-8 text-void/40 text-sm">
        Or just start building. Type anything to Claude — Claudia is watching.
      </p>
    </div>
  </section>

</Layout>

<style>
  /* Platform tabs */
  .tab-btn {
    padding: 0.375rem 0.75rem;
    font-size: 0.75rem;
    font-family: "JetBrains Mono", ui-monospace, monospace;
    color: rgba(250, 250, 248, 0.3);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 0.5rem;
    background: transparent;
    cursor: pointer;
    transition: all 0.2s;
  }
  .tab-btn:hover {
    color: rgba(250, 250, 248, 0.6);
    border-color: rgba(255, 255, 255, 0.1);
  }
  .tab-btn.active {
    color: var(--vermillion);
    border-color: rgba(230, 59, 46, 0.3);
    background: rgba(230, 59, 46, 0.05);
  }

  .tab-panel {
    display: none;
  }
  .tab-panel.active {
    display: block;
  }

  /* Keyboard keys */
  .kbd {
    display: inline-block;
    padding: 0.125rem 0.375rem;
    font-size: 0.6875rem;
    font-family: "JetBrains Mono", ui-monospace, monospace;
    color: rgba(250, 250, 248, 0.6);
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 0.25rem;
  }

  /* Troubleshooting accordion */
  .troubleshoot {
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 0.75rem;
    overflow: hidden;
  }
  .troubleshoot summary {
    padding: 0.75rem 1.25rem;
    font-size: 0.8125rem;
    color: rgba(250, 250, 248, 0.4);
    cursor: pointer;
    list-style: none;
    transition: color 0.2s;
  }
  .troubleshoot summary:hover {
    color: rgba(250, 250, 248, 0.6);
  }
  .troubleshoot summary::-webkit-details-marker {
    display: none;
  }
  .troubleshoot summary::before {
    content: "+";
    display: inline-block;
    width: 1.25rem;
    font-family: "JetBrains Mono", ui-monospace, monospace;
    font-weight: 500;
    color: var(--vermillion);
    opacity: 0.5;
  }
  .troubleshoot[open] summary::before {
    content: "−";
  }
  .troubleshoot[open] {
    border-color: rgba(255, 255, 255, 0.1);
  }
</style>

<script>
  // Platform tabs
  document.querySelectorAll("[data-tabs]").forEach((tabGroup) => {
    const buttons = tabGroup.querySelectorAll(".tab-btn");
    const panels = tabGroup.querySelectorAll(".tab-panel");

    buttons.forEach((btn) => {
      btn.addEventListener("click", () => {
        const target = btn.getAttribute("data-tab");
        buttons.forEach((b) => b.classList.remove("active"));
        panels.forEach((p) => p.classList.remove("active"));
        btn.classList.add("active");
        tabGroup.querySelector(`[data-panel="${target}"]`)?.classList.add("active");
      });
    });
  });

  // Auto-detect OS and activate correct tab
  const ua = navigator.userAgent.toLowerCase();
  let detectedOS = "mac";
  if (ua.includes("win")) detectedOS = "windows";
  else if (ua.includes("linux")) detectedOS = "linux";

  document.querySelectorAll("[data-tabs]").forEach((tabGroup) => {
    const buttons = tabGroup.querySelectorAll(".tab-btn");
    const panels = tabGroup.querySelectorAll(".tab-panel");

    buttons.forEach((btn) => {
      const tab = btn.getAttribute("data-tab");
      if (tab && (tab === detectedOS || tab.startsWith(detectedOS + "-"))) {
        buttons.forEach((b) => b.classList.remove("active"));
        panels.forEach((p) => p.classList.remove("active"));
        btn.classList.add("active");
        tabGroup.querySelector(`[data-panel="${tab}"]`)?.classList.add("active");
      }
    });
  });
</script>
